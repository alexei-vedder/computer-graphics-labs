{
	"name": "computer-graphics-labs",
	"version": "3.0.0",
	"scripts": {
		"serve": "parcel ./src/index.html --port=4321 --open",
		"build": "parcel build ./src/index.html",
		"deploy": "firebase deploy",
		"buildDeploy": "npm run build && npm run deploy"
	},
	"devDependencies": {
		"parcel-bundler": "1.12.4"
	},
	"dependencies": {
		"@types/mathjs": "^6.0.11",
		"file-saver": "^2.0.5",
		"mathjs": "^9.2.0",
		"obj-file-parser": "^0.5.1"
	}
}
